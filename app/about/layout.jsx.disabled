// this an example of a custom layout that applies to about and ALL it's children
const AboutLayout = ({ children }) => {
  return (
    <div>
      <h1>This is the About Layout</h1>
      {children}
    </div>
  );
};

export default AboutLayout;
